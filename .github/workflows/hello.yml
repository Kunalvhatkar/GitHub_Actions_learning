name: Hello
on:
  push:
    branches:
      - main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say heloo to users
        run: echo "Hello Kunal"
  details:
    runs-on: ubuntu-latest
    steps:
      - name: show version of python, docker, etc.
        run: docker --version && python --version 
